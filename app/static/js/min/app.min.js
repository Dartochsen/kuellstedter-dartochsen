js_content = """
function showAlert(message) {
    alert(message);
}

function toggleMenu() {
    const menu = document.getElementById('menu');
    menu.classList.toggle('open');
}

window.onload = function() {
    console.log('App initialized');
};
"""

# Minify the JavaScript content
minified_js = "".join([line.strip() for line in js_content.splitlines()])

# Save the minified JavaScript to a file
with open("app.min.js", "w") as js_file:
    js_file.write(minified_js)

print("app.min.js file created successfully.")
